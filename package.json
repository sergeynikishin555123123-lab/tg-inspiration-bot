{
  "name": "tg-inspiration-bot",
  "version": "1.0.0",
  "description": "Telegram bot with React Mini App for inspiration workshop",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.64.0",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.5.0"
  },
  "engines": {
    "node": "18.x"
  }
}
